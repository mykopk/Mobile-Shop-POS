export const UI = {
  loading: "Loading…",
  signingIn: "Signing in…",
  signIn: "Sign in",
  signOut: "Sign out",
  error: "Something went wrong",
  retry: "Try again",
} as const;

export const TOAST = {
  maxVisible: 3,
  durationMs: 3200,
} as const;
